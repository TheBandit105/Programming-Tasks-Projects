#include <stdio.h>
#include "my_coversion.h"
#include <stdlib.h>

int main(int argc, char ** argv) {

char * n2 = argv[2];
char * n1 = argv[1];
int s = 0;

sum = my_conversion(n1) + my_conversion(n2);
printf("%d\n", s);

  return 0;
}
